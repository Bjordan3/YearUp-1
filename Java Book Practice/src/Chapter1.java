// This is a class
public class Chapter1 {

	// This is a method. The main method
	public static void main(String[] args) {

		// This is a println
		System.out.println("My name is Jacquan");

		// These are few variable
		int a;
		int b = 1;
		int sum;
		double c = 3;
		float d;
		char e;
		String f;

		// This is assigning variables information
		a = 2;

		// This is using the operators to do simple math.
		sum = a + b;
		sum = a - b;
		sum = a / b;

		// This is an if statement
		if (a < 5) {
			System.out.println("A does not greater than five");
			a++;
		}
		a = 10;
		if (a > 5) {
			System.out.println("A does not less than five. A = " + a);
		}

		// This is a for loop
		int count;
		for (count = 0; count < 22; count = count + 1) {
			System.out.println("The count is " + count);
		}

		// this is a code block
		if (a < b) {
			sum = a + a + b;
			sum = b + b + a;
		}
		
		//these are all keywords
		/* abstract
		 * assert
		 * boolean primitive type
		 * break
		 * byte  -128-127 primitive type
		 * case
		 * catch
		 * char primitive type
		 * class
		 * const
		 * continue
		 * default
		 * do 
		 * double primitive type
		 * else
		 * enum
		 * extends
		 * final 
		 * finally
		 * float primitive type
		 * import
		 * instanceof
		 * int -2m-2m primitive type
		 * interface
		 * long -9^-18-9^18primitive type
		 * native
		 * new
		 * package
		 * private
		 * protected
		 * public
		 * return
		 * short -33k-33k primitive type
		 * static
		 * strictfp
		 * super
		 * switch
		 * synchronized
		 * this
		 * throw
		 * throws
		 * transient
		 * try
		 * void
		 * volatile
		 * while
		 */
		

	}

}
